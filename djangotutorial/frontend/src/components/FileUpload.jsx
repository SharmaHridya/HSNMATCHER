import { useRef } from 'react'
import { UploadCloud } from 'lucide-react'
export default function FileUpload({ accept, onFileSelect, disabled }) { const ref=useRef(null); return <div className="border border-dashed border-border bg-canvas p-6 text-center"><UploadCloud size={22} strokeWidth={1.6} className="mx-auto text-accent"/><p className="mt-3 text-sm text-ink">Drop a CSV here, or <button type="button" disabled={disabled} onClick={()=>ref.current?.click()} className="text-accent underline underline-offset-2">choose a file</button></p><p className="mt-1 text-xs text-ink-muted">Include a description column for best results.</p><input ref={ref} onChange={e=>onFileSelect(e.target.files?.[0]||null)} accept={accept} disabled={disabled} type="file" className="sr-only"/></div> }
